import * as React from "react";
import { Box } from "@mui/material";

interface CardZoneProps {
  initialCards: Array<Number>;
  renderCards: Function;
  sortable: boolean;
}

const CardZone = ({}: CardZoneProps) => {
  return <Box sx={{}}></Box>;
};

export default CardZone;
